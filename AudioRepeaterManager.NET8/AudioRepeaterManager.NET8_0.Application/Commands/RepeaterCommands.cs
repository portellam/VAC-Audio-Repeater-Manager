namespace AudioRepeaterManager.NET8_0.Application.Commands
{
  public class RepeaterCommands
  {
    /*
     * TODO:
     * 
     * -add command logic
     *  -start a repeater; starts the repeater process.
     *  -stop a repeater; stops the repeater process.
     *  -restart a repeater; restarts the repeater process.
     * 
     */
  }
}
